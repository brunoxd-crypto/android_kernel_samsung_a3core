name: Compilar_Kernel_A03_V11_Final
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:20.04

    steps:
      - name: Instalar_Dependencias
        run: |
          apt-get update
          export DEBIAN_FRONTEND=noninteractive
          apt-get install -y git build-essential bc bison flex libssl-dev \
            libncurses5-dev libncurses5 libtinfo5 python3 python2 \
            make zip liblz4-tool lzop curl wget tar
          ln -sf /usr/bin/python2 /usr/bin/python

      - name: Descargar_Compilador_Neutron_Clang
        run: |
          # Neutron es un reemplazo directo de Zyc, muy usado en MTK y Unisoc
          # Lo bajamos como archivo comprimido para evitar errores de Git Username
          mkdir /zyc
          wget -q https://gist.githubusercontent.com/dakkshesh07/2ca3ca6463838449a6140c6095034b08/raw/neutron-clang.sh
          bash neutron-clang.sh
          # El script lo descarga en una carpeta llamada 'neutron', la movemos
          mv /source/neutron/* /zyc/ || true
          
      - name: Descargar_GCC_Lineage
        run: |
          # Estos siguen siendo públicos, pero por si acaso forzamos la URL limpia
          git clone --depth 1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9.git /gcc64
          git clone --depth 1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_arm_arm-linux-androideabi-4.9.git /gcc32

      - name: Descargar_Fuentes_IverCoder
        run: |
          # Usamos tu link directo
          git clone --depth 1 -b upstreaming https://github.com/brunoxd-crypto/android_kernel_samsung_a3core.git /source

      - name: Compilar_Con_Neutron
        run: |
          # Ajustamos el PATH al nuevo compilador
          export PATH="/zyc/bin:/gcc64/bin:/gcc32/bin:$PATH"
          cd /source
          mkdir -p out
          
          # Configurar
          make O=out ARCH=arm64 a3core_eur_open_defconfig
          
          # Parche Stack Protector
          sed -i 's/CONFIG_CC_STACKPROTECTOR_STRONG=y/# CONFIG_CC_STACKPROTECTOR_STRONG is not set/g' out/.config
          echo "CONFIG_CC_STACKPROTECTOR_NONE=y" >> out/.config

          # Compilación
          make -j$(nproc) O=out \
              ARCH=arm64 \
              CC=clang \
              CLANG_TRIPLE=aarch64-linux-gnu- \
              CROSS_COMPILE=aarch64-linux-android- \
              CROSS_COMPILE_ARM32=arm-linux-androideabi- \
              LD=aarch64-linux-android-ld \
              AR=aarch64-linux-android-ar \
              NM=aarch64-linux-android-nm \
              STRIP=aarch64-linux-android-strip \
              OBJCOPY=aarch64-linux-android-objcopy \
              KCFLAGS="-w" \
              V=1

      - name: Subir_Resultado
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Kernel_A03_Upstreaming_V11
          path: /source/out/arch/arm64/boot/Image*
